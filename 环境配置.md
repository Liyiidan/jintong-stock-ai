# 金通科技运行配置

## 推荐方式：Docker

需要先安装并启动 Docker Desktop。

```bash
cp .env.example .env
docker compose up --build
```

首次启动需要下载 PostgreSQL、Python、Node 和项目依赖，耗时取决于网络。服务健康后访问：

- 前端：http://localhost:8080
- Swagger：http://localhost:8000/docs
- API 健康检查：http://localhost:8000/

停止服务：

```bash
docker compose down
```

数据库保存在 Docker volume `db_data` 中，普通的 `docker compose down` 不会删除数据。

## 环境变量

根目录 `.env`：

```dotenv
POSTGRES_USER=postgres
POSTGRES_PASSWORD=请设置本机数据库密码
POSTGRES_DB=stockai
POSTGRES_PORT=5433
JWT_SECRET=请设置至少32位随机字符串
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080
DATA_MODE=real
INSTALL_DATA_DEPS=false
DATA_REQUEST_TIMEOUT_SECONDS=25
ZHIPU_API_KEY=
ZHIPU_ALLOW_CROSS_ROLE_KEY_FALLBACK=true
SEED_DEMO_DATA=false
DEMO_USER_EMAIL=demo@jintong.example.com
DEMO_USER_PASSWORD=Demo123456!
```

账户注册、登录、JWT 和画像功能不需要智谱 API Key。只有调用 LLM 分析时才需要设置 `ZHIPU_API_KEY`。不要提交真实 `.env`，也不要复用旧仓库中公开过的 Key。

Docker 默认使用 `requirements-core.txt`，不安装重量级数据/模型依赖。需要启用 AkShare 时，将 `INSTALL_DATA_DEPS=true`，然后重新构建后端；它只会安装 `requirements-data.txt` 中的 AkShare，不会安装 Transformers/PyTorch。

后端会通过 `GET /api/v1/system/capabilities` 返回能力状态。未配置智谱 Key 不会导致 FastAPI 启动失败；页面会明确显示 AI 未配置，并保留规则分析回退。

真实模式默认不会写入演示账号或样例行情。只有同时设置 `DATA_MODE=demo` 和 `SEED_DEMO_DATA=true` 才会初始化样例数据库。推荐使用项目根目录的 `docker-compose.demo.yml` 运行演示环境；真实模式不要复用演示环境的数据库 volume。

后端 `GET /api/v1/system/capabilities` 还会返回每个数据集的记录数、来源字段、最后更新时间和最近同步失败信息。真实模式检测到旧 volume 中残留的 `demo_seed` 行时，首页会显示“样例数据需清理”。

配置智谱 Key 后，先用 `docker compose exec backend python scripts/check_zhipu.py` 验证 SDK 和 Key，再重建后端。分析结果会保存模型、分析模式、数据覆盖和回退专家信息；缺少 Key 或调用失败时只使用明确标记的规则回退。

启用 AkShare 后，建议先运行受限试验集：

```bash
docker compose exec backend python scripts/sync_minimal_real.py 000001 000333 000651
```

该脚本只接受 3 至 5 只股票，便于逐项核对报价、K 线、财报和新闻的来源与更新时间。

## 单独启动前端

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:5173。此模式默认请求 http://localhost:8000/api/v1，因此后端也需要运行。

仅预览界面和固定演示数据：

```bash
npm run dev:demo
```

## 单独启动后端

后端目标版本为 Python 3.11，并需要可连接的 PostgreSQL 16。

```bash
cd backend
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python scripts/init_db.py
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

`backend/.env` 默认数据库连接为：

```dotenv
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/stockai
```

请让其中的用户名和密码与本机 PostgreSQL 实际配置一致，并为 `JWT_SECRET` 设置随机值。
